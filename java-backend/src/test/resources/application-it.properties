# This file is necessary because you can't directly use YAML files in @TestPropertySource
spring.config.import=classpath:application.yml,classpath:application-test.yml,optional:config/local/application-test.yml
